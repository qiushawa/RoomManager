# =============================================================================
# 教室借用管理系統 (Room Manager) 環境設定
# =============================================================================
# 複製此檔案並命名為 .env，然後根據您的環境調整設定
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# 應用程式基本設定
# -----------------------------------------------------------------------------
APP_NAME="教室借用系統"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
# 若使用 XAMPP，請設定為: http://localhost/RoomManager/public
# 若使用 php artisan serve，請設定為: http://localhost:8000
APP_URL=http://localhost
# 若使用 XAMPP，需額外設定 ASSET_URL
# ASSET_URL=http://localhost/RoomManager/public

# 語系設定 (zh_TW = 繁體中文)
APP_LOCALE=zh_TW
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=zh_TW

# 維護模式設定
APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP CLI 工作程序數量
PHP_CLI_SERVER_WORKERS=4

# -----------------------------------------------------------------------------
# 安全性設定
# -----------------------------------------------------------------------------
BCRYPT_ROUNDS=12

# -----------------------------------------------------------------------------
# 日誌設定
# -----------------------------------------------------------------------------
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# -----------------------------------------------------------------------------
# 資料庫設定
# -----------------------------------------------------------------------------
# MySQL (建議用於生產環境)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=room_manager
DB_USERNAME=root
DB_PASSWORD=

# SQLite (適合開發或測試)
# DB_CONNECTION=sqlite
# DB_DATABASE=/absolute/path/to/database.sqlite

# -----------------------------------------------------------------------------
# Session 設定
# -----------------------------------------------------------------------------
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# -----------------------------------------------------------------------------
# 服務驅動設定
# -----------------------------------------------------------------------------
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
CACHE_STORE=database
# CACHE_PREFIX=

# -----------------------------------------------------------------------------
# Redis 設定 (若使用 Redis)
# -----------------------------------------------------------------------------
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Memcached 設定 (若使用 Memcached)
MEMCACHED_HOST=127.0.0.1

# -----------------------------------------------------------------------------
# 郵件設定
# -----------------------------------------------------------------------------
# 開發環境建議使用 log 或 Mailtrap
# 生產環境可使用 smtp, sendmail, mailgun, ses 等
MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=587
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# -----------------------------------------------------------------------------
# AWS 設定 (若使用 AWS 服務)
# -----------------------------------------------------------------------------
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=ap-northeast-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# -----------------------------------------------------------------------------
# Vite 前端設定
# -----------------------------------------------------------------------------
VITE_APP_NAME="${APP_NAME}"
